[package]
name = "creature-context-macros"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2.0", features = ["full"] }
quote = "1.0"
proc-macro2 = "1.0"
creature-context-store = { version = "0.1.0", path = "../context-store" }
creature-context-types = { version = "0.1.0", path = "../context-types" }
